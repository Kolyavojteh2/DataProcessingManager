#include "DataProcessingManager.h"


/*
template<class Key_t, class HandlerFunc>
class DataProcessingManager
{
public:
    DataProcessingManager();

    int add();
    int remove();

    void runAll();
    void run(const Key_t& key);

private:
    std::map<Key_t, HandlerFunc> m_handlers;
};
*/

